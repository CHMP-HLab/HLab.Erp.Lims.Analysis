<UserControl x:Class="HLab.Erp.Lims.Analysis.Module.SampleTests.SampleTestDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:base="clr-namespace:HLab.Base;assembly=HLab.Base.Wpf"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HLab.Erp.Lims.Analysis.Module"
             xmlns:lang="clr-namespace:HLab.Mvvm.Lang;assembly=HLab.Mvvm.Wpf"
             xmlns:icons="clr-namespace:HLab.Mvvm.Icons;assembly=HLab.Mvvm.Wpf"
             xmlns:sampleTests="clr-namespace:HLab.Erp.Lims.Analysis.Module.SampleTests"
             xmlns:mvvm="clr-namespace:HLab.Mvvm;assembly=HLab.Mvvm.Wpf"
             xmlns:annotations="clr-namespace:HLab.Mvvm.Annotations;assembly=HLab.Mvvm.Annotations"
             Background="#FF005040"
             d:DataContext="{d:DesignInstance {x:Type sampleTests:SampleTestViewModelDesign}, IsDesignTimeCreatable=True}"             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <base:DetailView>
            <mvvm:ViewLocator Height="50" Model="{Binding Locker}" Grid.ColumnSpan="2"/>

            <Label>
                <lang:Localize Id="{}{Workflow}" Margin="50,0,0,0"/>
            </Label>
            <ContentPresenter Content="{Binding Workflow}"/>

                <Label Style="{DynamicResource Separator}" HorizontalContentAlignment="Stretch">
            <Grid VerticalAlignment="Center">
                <Canvas DockPanel.Dock="Left">
                    <icons:IconView Path = "{Binding Model.TestClass.IconPath}" Height="35" Opacity="0.8"/>
                </Canvas>
                <!--<lang:Localize Id="Test" VerticalAlignment="Center" Margin="50,0,0,0"/>-->
                <lang:Localize Id="{Binding Model.TestClass.Name}" Margin="50,0,0,0"/>
            </Grid></Label>

            <Label HorizontalContentAlignment="Right" >
                <lang:Localize Id="{}{Type}" StringFormat="{}{0} : " />
            </Label>
            <TextBox Text="{Binding Model.}" HorizontalContentAlignment="Center" />

            <Label HorizontalContentAlignment="Right" >
                <lang:Localize Id="{}{Method} : "/>
            </Label>
            <TextBox Text="{Binding Model.Method}" HorizontalContentAlignment="Center" />

            <Label Style="{DynamicResource Separator}" HorizontalContentAlignment="Stretch">
                <Grid VerticalAlignment="Center">
                    <Canvas DockPanel.Dock="Left">
                        <icons:IconView Path = "Icons/Result" Height="35" Opacity="0.8"/>
                    </Canvas>
                    <lang:Localize Id="{}{Results}" VerticalAlignment="Center" Margin="50,0,0,0"/>                
                </Grid>
            </Label>

            <Label></Label>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Command="{Binding ViewSpecificationsCommand}" HorizontalAlignment="Left">
                        <lang:Localize Id="{}{Specifications}"/>
                    </Button>                    
                    <Button Command="{Binding AddResultCommand}" HorizontalAlignment="Left">
                        <lang:Localize Id="{}{Add result}"/>
                    </Button>                    
                    <Button Command="{Binding RemoveResultCommand}" HorizontalAlignment="Left">
                        <lang:Localize Id="{}{Remove result}"/>
                    </Button>                    
                    <Button Command="{Binding SelectResultCommand}" HorizontalAlignment="Left">
                        <lang:Localize Id="{}{Select result}"/>
                    </Button>                    
                </StackPanel>
                <Grid VerticalAlignment="Top" mvvm:ViewLocator.ViewClass="{x:Type annotations:IViewClassContent}">
                    <mvvm:ViewLocator Model="{Binding Results}"/>
                </Grid>
            </StackPanel>
            
            <Grid Grid.ColumnSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
            
                <Label HorizontalContentAlignment="Right" >
                    <lang:Localize Id="{}{Description} : "/>
                </Label>
                <TextBlock HorizontalAlignment="Center" Text="{Binding Model.Description}" Grid.Row="1"/>
            
                <Label HorizontalContentAlignment="Center" Grid.Column="1" >
                    <lang:Localize Id="{}{Result}"/>
                </Label>
                <TextBlock HorizontalAlignment="Center" Text="{Binding Model.Result}" Grid.Row="1" Grid.Column="1"/>
            
                <Label HorizontalContentAlignment="Center" Grid.Column="2" >
                    <lang:Localize Id="{}{Conformity}"/>
                </Label>
                <TextBlock HorizontalAlignment="Center" Text="{Binding Model.Conform}" Grid.Row="1" Grid.Column="2"/>
                <Label HorizontalContentAlignment="Center" Grid.Column="3" >
                    <lang:Localize Id="{}{Conformity}"/>
                </Label>
                <TextBlock HorizontalAlignment="Center" Text="{Binding Model.Conform}" Grid.Row="1" Grid.Column="3"/>
            </Grid>
            

            <Label Style="{DynamicResource Separator}" HorizontalContentAlignment="Stretch">
                <Grid VerticalAlignment="Center">
                    <Canvas DockPanel.Dock="Left">
                        <icons:IconView Path = "Icons/Sample/Folder" Height="35" Opacity="0.8"/>
                    </Canvas>
                    <lang:Localize Id="{}{Documents}" VerticalAlignment="Center" Margin="50,0,0,0"/>
                </Grid>
            </Label>

        </base:DetailView>
    </Grid>
</UserControl>
